version: "3.9"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile-server
    ports:
      - "8888:8888"
    networks:
      - app-network
    restart: always


networks:
  app-network:
    driver: bridge